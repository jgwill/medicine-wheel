future docker container repo `jgwill/medicine-wheel`

Study `/src/coaia-visualizer` and how the main app is published as a CLI that we can run with an input JSONL and can also be launched with --docker, analogical to that, we'd supply the path of the project we want a .mw/store folder being created (or exist) so we can launch the service and connect the MCP to it.  
* * It might imply creating also a docker container for the MCP service so all talk to each other well.  The goal is that on the network, we are capable to connect all agent to the medicine-wheel app and potentially have many of them opened in different workspace (project location).


