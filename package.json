{
  "name": "medicine-wheel",
  "version": "1.0.0",
  "private": true,
  "description": "Medicine Wheel — Interactive visual layer for Indigenous relational research with Four Directions, ceremonies, and narrative arcs",
  "scripts": {
    "build:packages": "npm run build -w medicine-wheel-ontology-core && npm run build --workspaces --if-present",
    "clean:packages": "npm run clean --workspaces --if-present",
    "prebuild": "npm run build:packages",
    "predev": "npm run build:packages",
    "dev": "next dev -p 3940",
    "build": "next build",
    "start": "next start -p 3940",
    "lint": "next lint",
    "version:patch": "npm version patch -ws --no-git-tag-version && node scripts/sync-versions.mjs && shx rm -rf node_modules src/*/node_modules package-lock.json && npm install --legacy-peer-deps",
    "version:minor": "npm version minor -ws --no-git-tag-version && node scripts/sync-versions.mjs && shx rm -rf node_modules src/*/node_modules package-lock.json && npm install --legacy-peer-deps",
    "version:major": "npm version major -ws --no-git-tag-version && node scripts/sync-versions.mjs && shx rm -rf node_modules src/*/node_modules package-lock.json && npm install --legacy-peer-deps",
    "version:set": "npm version -ws",
    "sync:versions": "node scripts/sync-versions.mjs",
    "prepublishOnly": "npm run clean:packages && npm run build:packages",
    "publish:packages": "npm run prepublishOnly && npm publish -ws --access public",
    "publish:dry": "npm run prepublishOnly && npm publish -ws --access public --dry-run"
  },
  "dependencies": {
    "next": "^15.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "next-themes": "^0.4.6",
    "sonner": "^1.7.0",
    "lucide-react": "^0.475.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.0.2",
    "recharts": "^2.15.4",
    "medicine-wheel-ontology-core": "^0.2.0",
    "medicine-wheel-ceremony-protocol": "^0.2.0",
    "medicine-wheel-narrative-engine": "^0.2.0",
    "medicine-wheel-graph-viz": "^0.2.0",
    "medicine-wheel-relational-query": "^0.2.0",
    "medicine-wheel-prompt-decomposition": "^0.2.0",
    "medicine-wheel-ui-components": "^0.2.0",
    "medicine-wheel-data-store": "^0.2.0",
    "medicine-wheel-session-reader": "^0.2.0",
    "medicine-wheel-fire-keeper": "^0.2.0",
    "medicine-wheel-importance-unit": "^0.2.0",
    "medicine-wheel-relational-index": "^0.2.0",
    "medicine-wheel-transformation-tracker": "^0.2.0",
    "medicine-wheel-storage-provider": "^0.1.0",
    "@neondatabase/serverless": "^0.10.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "shx": "^0.4.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.7.0"
  }
}
