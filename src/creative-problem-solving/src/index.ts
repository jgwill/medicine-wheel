/**
 * @medicine-wheel/creative-problem-solving
 *
 * You arrived here looking for creative problem solving. This package is a
 * signpost, not a door — it holds no logic of its own and never will. It
 * carries one question and then sends you one of two ways.
 *
 *   **Is there a prior state you are restoring?**
 *
 * Name it and it genuinely existed — this is a fire, and `gap-analysis` is the
 * right instrument. Baseline, observation, difference, elimination steps you
 * can verify. That work is correct work; there is nothing to apologise for and
 * nothing to rename.
 *
 * Name none — you are creating, and structural tension is the instrument.
 * Difference-closing language will mislead you here, because there is no
 * baseline to return toward: an outcome phrased as removal optimises against a
 * list of known failures rather than toward the state you actually want.
 *
 * A feeling of urgency is not evidence. Under pressure every situation feels
 * like a fire, which is why the question is asked before the work rather than
 * after it.
 *
 * Everything exported here comes from `@medicine-wheel/creative-orientation` —
 * the package that answers the question. Import either name; they are the same
 * surface. This one exists because it is the name you were looking for.
 *
 * @packageDocumentation
 */

export * from '@medicine-wheel/creative-orientation';

/** The question this package exists to ask, before any of the work begins. */
export const THE_QUESTION = 'Is there a prior state you are restoring?' as const;
