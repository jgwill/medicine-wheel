# Prompt Decomposition

> Engine: **claude** (opus)

## Four Directions

### 🌅 EAST — Vision

- Draft the mission memory immediately so the full ask survives the session — the explicit 'draft something right away to remember well' instruction [88%]
- Receive the correction: infrastructure has a topology, not an apology — name what the skills must describe [95%]
- Envision the user interface that conveys infrastructure topology, ceremony relation, and per-host perspectives [90%]
- Name the desired result: infra moves from zero consumers to a door agents can walk through [85%] _(implicit)_

### 🔥 SOUTH — Analysis

- Read the artefact's 6 proposed MCP actions and 5 tool upgrades as the implementation spec [90%]
- Identify the lowest supporting layers and their dependency order (topological workspaces array) [88%]
- Study RELEASING.md before touching any version — publishing is not deploying [90%] _(implicit)_
- Study cli/skills.ts to see exactly what infra-monitor claims versus what exists [90%]
- Learn the RISE framework requirements for the rispec stub [82%] _(implicit)_
- Map the many perspectives — per ceremony and per host — that the topology model must hold [85%]

### 🌊 WEST — Validation

- Test everything: new MCP actions, upgraded tools, corrected skill records [95%]
- Spawn at least one sub-agent to review the work and recommend robustness fixes [95%]
- Install the published packages globally and run the installed binary — a green publish proves nothing [88%] _(implicit)_
- Verify @medicine-wheel/mcp is included in the publish set despite its 4.x line [90%] _(implicit)_
- Confirm no new NodeType was introduced — services ride knowledge nodes with metadata.kind [80%] _(implicit)_
- Apply the sub-agent's robustness recommendations before declaring the mission complete [82%] _(implicit)_

### ❄️ NORTH — Action

- Implement the 6 new MCP actions and 5 tool upgrades, starting at the lowest layers [92%]
- Declare @medicine-wheel/infra in mcp/package.json and wire detectPortConflicts to a reachable tool [87%] _(implicit)_
- Rewrite the infra-monitor skill record to describe topology honestly [90%]
- Bump all packages in lockstep and publish the full suite [90%]
- Print the UI recommendations as mission-completion output [93%]
- Author and commit the RISE rispec stub for the user interface [93%]
- Upgrade the artefact with the UI + topology + ceremony + per-host section [90%]

## Original Prompt

> # Input prompt — verbatim
> 
> Received 2026-08-05, session `mw-infra-mcp`, from William (ava@jgwill.com), spoken/transcribed.
> 
> > that's pretty great we really are on the way to do exactly that so I want you to implement
> > everything test everything make sure the skills that tells users about the infrastructure
> > apology or also upgraded so there is a not infrastructure apology but infrastructure topology,
> > I'm expecting all the packages to be upgraded published tested and you'll use at least one sub
> > agent to review your work and suggest I recommend things that you need to to fix and make it
> > more robust and when you complete your mission your print out some recommendations for creating
> > a new user interface with one stub in ./rispecs using RISE framework that will convey your whole
> > user interface and this is going to be committed as well (given that it will be in relationship
> > to everything that you did so far, you'll be well placed to do that). At that moment you will
> > upgrade the artefact with one more section that presents the user interface that you envisioned
> > to create or more likely upgrade or whatever present infrastructure topologies and their
> > relation to a ceremony as well as per hosts (many perspectives)). if you need to draft something
> > right away to remember well when I'm asking you to do you'll do that right away and then you'll
> > start working on the lowest layers that supports that
> 
> ## Prior turn this builds on
> 
> The artefact published at `https://claude.ai/code/artifact/7f8534b8-60c1-4db8-b62a-e910a6d298c4`
> — *"@medicine-wheel/infra — the vocabulary and the missing door"* — which established, measured
> against the working tree on 2026-08-05:
> 
> - `@medicine-wheel/infra@0.5.7` ships 409 lines, 3 facets, 1 pure function, 11 Zod schemas.
> - **Zero consumers.** `infra` is absent from `mcp/package.json` dependencies (16 `@medicine-wheel/*`
>   deps declared, not this one). The CLI names it twice, both as prose inside a skill record.
> - `detectPortConflicts` is reachable from `tests/infra-port-conflicts.test.ts` and from no agent,
>   no tool, no route.
> - The artefact proposed **6 new MCP actions and 5 upgrades** to shipped tools.
> 
> A voice tour of that artefact was published as message `cf66ab5d-18ec-4913-adc6-c6e6189e0ac7`
> (persona `nyro`, 3:25) — binding `unresolved`, no episode.
> 
> ## Transcription note
> 
> "infrastructure apology" is a transcription artefact for **infrastructure topology**. The
> correction is itself part of the ask: the `infra-monitor` skill record currently describes a
> capability that does not exist (it claims systemd monitoring; `cli/skills.ts` defines it as a
> document with no runtime). Read plainly: *the skills must stop apologising for infrastructure
> and start describing its topology.*
> 

## Primary Intent

**Action:** implement
**Target:** the 6 new MCP actions and 5 tool upgrades proposed in the @medicine-wheel/infra artefact, wiring infra from zero consumers to a reachable door (MCP + CLI)
**Urgency:** immediate
**Confidence:** 92%

## Secondary Intents

1. **draft** — an immediate memory artefact capturing the full mission before any implementation begins (the 'if you need to draft something right away to remember well' instruction) _(explicit)_
2. **start** — work at the lowest supporting layers first (ontology-core / infra types → data-store → mcp → cli → ui) _(explicit)_
   - depends on: draft of mission memory
3. **add** — @medicine-wheel/infra as a declared dependency in mcp/package.json (currently absent among 16 @medicine-wheel/* deps) _(implicit)_
   - depends on: lowest-layer work started
4. **expose** — detectPortConflicts through an agent-reachable MCP tool and/or CLI route _(implicit)_
   - depends on: infra declared as mcp dependency
5. **rename** — the mis-transcribed 'infrastructure apology' concept to 'infrastructure topology' everywhere skills describe infrastructure _(explicit)_
6. **correct** — the infra-monitor skill record in cli/skills.ts, which claims systemd monitoring but is a document with no runtime — make it describe topology honestly under 'Planned invocation (does not run yet)' _(implicit)_
   - depends on: rename apology→topology
7. **test** — everything — new MCP actions, upgraded tools, skill records, infra functions _(explicit)_
   - depends on: implementation of MCP actions and skill upgrades
8. **bump** — all suite packages in lockstep on 0.x, with @medicine-wheel/mcp tracking its own 4.x major _(explicit)_
   - depends on: tests passing
9. **publish** — all packages including @medicine-wheel/mcp (never skipped for being on a different version line) _(explicit)_
   - depends on: version bump
10. **install** — the published packages globally and run the installed binary to prove the release actually works _(implicit)_
   - depends on: publish
11. **spawn** — at least one sub-agent to review the work and recommend robustness fixes _(explicit)_
   - depends on: implementation complete
12. **apply** — the sub-agent's recommended fixes to make the implementation more robust _(implicit)_
   - depends on: sub-agent review
13. **print** — recommendations for a new user interface at mission completion _(explicit)_
   - depends on: mission complete
14. **author** — one RISE-framework stub spec in ./rispecs conveying the whole user interface _(explicit)_
   - depends on: UI recommendations printed
15. **commit** — the rispec stub (explicitly stated as 'going to be committed as well') _(explicit)_
   - depends on: rispec stub authored
16. **upgrade** — the published artefact 7f8534b8 with one additional section presenting the envisioned UI, infrastructure topologies, their relation to ceremony, and per-host perspectives _(explicit)_
   - depends on: rispec stub committed
17. **model** — topology from many perspectives — per ceremony and per host — as the artefact section's organising structure _(explicit)_
   - depends on: artefact upgrade started
18. **register** — infrastructure as knowledge nodes with metadata.kind:'service' rather than introducing a new NodeType (the union stays closed at six) _(implicit)_
   - depends on: lowest-layer work started
19. **resolve** — the unresolved binding on voice-tour message cf66ab5d (no episode attached) _(implicit)_

## Context Requirements

### Files Needed
- src/infra/src/*.ts
- src/mcp/package.json
- src/mcp/src/tools/*
- src/cli/skills.ts
- src/ontology-core/src/types.ts
- src/ontology-core/src/constants.ts
- tests/infra-port-conflicts.test.ts
- package.json
- RELEASING.md
- CLAUDE.md
- scripts/bump-versions.mjs
- rispecs/

### Tools Required
- Read
- Edit
- Write
- Bash
- Grep
- Glob
- Agent
- Artifact

### Assumptions
- 'infrastructure apology' is a transcription artefact and the intended word is 'topology'
- all packages are ready to be upgraded and published together in lockstep
- the agent, having done the implementation, is 'well placed' to design the UI recommendations
- the artefact 7f8534b8 remains editable and can receive an additional section
- the 6 proposed MCP actions and 5 tool upgrades from the artefact are the correct implementation target
- the lowest layers can be identified without further specification of which they are
- a single sub-agent review is sufficient ('at least one')
- publishing will succeed and the installed binary will behave correctly

## Expected Outputs

### Artifacts
- mission memory draft (immediate, before implementation)
- ./rispecs/<ui>.spec.md — RISE-framework UI stub
- new MCP tool/action modules for infra (6 actions)
- tests for the new MCP actions and upgraded tools

### Updates
- src/mcp/package.json — declare @medicine-wheel/infra dependency
- src/cli/skills.ts — infra-monitor record: topology not apology, honest runtime claim
- 5 shipped MCP tools receiving upgrades
- all package.json version fields (lockstep 0.x, mcp 4.x)
- artefact 7f8534b8 — new section on UI + topologies + ceremony relation + per-host perspectives

### Communications
- published npm packages (full suite including @medicine-wheel/mcp)
- commit containing the rispec stub
- printed UI recommendations at mission completion
- updated public artefact

## Action Stack

- [ ] Draft the mission memory artefact right away, before any code — capture the full ask verbatim so nothing is lost mid-mission
- [ ] Read RELEASING.md, CLAUDE.md, and the artefact's 6 actions / 5 upgrades to fix the implementation target (depends on: mission memory drafted)
- [ ] Survey the lowest supporting layers: infra types, ontology-core node/metadata contract, workspaces topological order (depends on: artefact spec read)
- [ ] Read cli/skills.ts infra-monitor record and every place a skill describes infrastructure (depends on: artefact spec read)
- [ ] Implement lowest-layer changes first: infra facets and any ontology-core support, services as knowledge nodes with metadata.kind:'service' (depends on: lowest layers surveyed)
- [ ] Declare @medicine-wheel/infra in mcp/package.json (depends on: lowest-layer changes implemented)
- [ ] Implement the 6 new MCP actions, including a route that reaches detectPortConflicts (depends on: infra declared in mcp deps)
- [ ] Apply the 5 upgrades to the already-shipped MCP tools (depends on: 6 new MCP actions implemented)
- [ ] Rewrite the infra-monitor skill record: topology not apology, and no claimed runtime that does not exist (depends on: lowest layers surveyed)
- [ ] Write and run tests for the new actions, the upgraded tools, and the corrected skill records (depends on: all implementation complete)
- [ ] Spawn at least one sub-agent to review the work and recommend robustness fixes (depends on: tests passing)
- [ ] Apply the sub-agent's robustness recommendations (depends on: sub-agent review returned)
- [ ] Bump all package versions in lockstep (suite 0.x, mcp 4.x tracking minor/patch) (depends on: robustness fixes applied)
- [ ] Publish all packages, verifying @medicine-wheel/mcp is in the set (depends on: versions bumped)
- [ ] Install globally and run the installed binary to prove the release works on a clean tree (depends on: publish complete)
- [ ] Print the user interface recommendations (depends on: release verified)
- [ ] Author the RISE-framework UI stub in ./rispecs conveying the whole user interface (depends on: UI recommendations printed)
- [ ] Commit the rispec stub (depends on: rispec stub authored)
- [ ] Upgrade the artefact with a section on the envisioned UI, infrastructure topologies, their ceremony relation, and per-host perspectives (depends on: rispec stub committed)

## Ambiguity Flags

- **"'implement everything' — the scope is unbounded without naming which of the artefact's 6 actions and 5 upgrades are in scope"**
  - Suggestion: Confirm the implementation target is exactly the artefact's 6 proposed MCP actions plus 5 tool upgrades, and nothing beyond
- **"'the skills that tells users about the infrastructure' — plural, but only infra-monitor is identified"**
  - Suggestion: Enumerate every skill record in cli/skills.ts that mentions infrastructure and confirm which need the topology correction
- **"'you'll start working on the lowest layers that supports that' — which layers count as lowest is not stated"**
  - Suggestion: Propose the dependency order (ontology-core → infra → data-store → mcp → cli → ui) and confirm before implementing
- **"'one stub in ./rispecs' — whether ./rispecs is the medicine-wheel repo's rispecs/ or the workspace-root rispecs/ is unstated"**
  - Suggestion: Confirm the target directory: /workspace/repos/jgwill/medicine-wheel/rispecs/ versus /workspace/rispecs/
- **"'upgrade the artefact with one more section' — whether this means editing the published artefact 7f8534b8 or publishing a successor"**
  - Suggestion: Confirm whether to republish artefact 7f8534b8 in place or issue a new versioned artefact linking back to it
- **"'or more likely upgrade or whatever present infrastructure topologies' — unclear whether an existing UI is being upgraded or a new one built"**
  - Suggestion: Ask whether an existing surface (mw-ui, graph canvas) is the upgrade target or the UI is greenfield
- **"'many perspectives' for per-host and per-ceremony views is not enumerated"**
  - Suggestion: List the concrete perspectives to model (per ceremony, per host, per port, per service lifecycle) and confirm the set
- **"'suggest I recommend things that you need to to fix' — transcription noise; whether the sub-agent's recommendations must be applied or only reported"**
  - Suggestion: Confirm that sub-agent recommendations are to be applied in this mission, not deferred to a follow-up
- **"The unresolved binding on voice-tour message cf66ab5d has no episode and no stated disposition in this prompt"**
  - Suggestion: Confirm whether resolving that binding or creating its episode is part of this mission
