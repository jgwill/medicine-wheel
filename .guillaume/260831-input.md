looking at `https://github.com/jgwill/medicine-wheel/issues/86` and """[film-production] MCP tool surface for perception / narrative-cluster / storyteller / production-ceremony #89
[film-production] Runtime Zod schemas for ProductionRelation and ProductionEntityKind #90
[film-production] Provenance + OCAP consent review for the episode-066 transcript fixture #91"""


directly related:
```
/srv/miadi/episodes/miadi-chronicle/2026-06-19-episode-067-renaud-meeting
/srv/miadi/episodes/miadi-chronicle/2026-06-19-episode-068-extending-medicine-wheel-for-relational-film-production
```

potentially related 
```
/srv/miadi/episodes/miadi-chronicle/2026-06-28-episode-103-film-preprod-report-phase-2
/srv/miadi/episodes/miadi-chronicle/2026-07-10-episode-120-relational-film-production-media-editor
/srv/miadi/episodes/miadi-chronicle/2026-08-17-episode-336-three-doors-into-one-film
```

needing to consult community but how ?

What is the place of `/a/src/Miadi/packages/review-service` in all that ?


Obviously `src/community-review/` needs consideration, the `/a/src/Miadi/packages/review-service` seems to be an API and a Web-UI interfaces on what the `src/community-review/` (`@medicine-wheel/community-review`) package is ?  Reflection with 3 subagents would lead to implement new packages or upgrade of packages (could migrate/absorbe fully the `/a/src/Miadi/packages/review-service` in the medicine-wheel). Consensus with the subagents would lead to implementing and publishing, commiting so that I would be capable to support what would happen in the community which still some unknown exist but I feel we can advance what we currently have given that it does not fully support what is needed...

Also, the idea behind `/a/src/Miadi/packages/inquiry-weave` needs to have a relation to what we are reviewing given that we relate something from a repo to a new episode which somehow is what we make the community-review participate in (they look at an episode...  )
* What shall we do, migrate some of the logics from it into the medicine-wheel then consume that package into  `/a/src/Miadi/packages/inquiry-weave` and publish both !?
* Anything else from the `/a/src/Miadi/packages/inquiry-weave` that we need to consider in the review-service or the community-review package ?

 and if that's not enough so far, `/src/Miadi/app/chronicle` does provide a user interface that inside of the medicine we are we don't really have something to view episodes/Chronicles either anything that give access to community of users....  what about a larger mission to abstract and implement the same as `/src/STPB/app` and everything that relates to the whole user participation inside of the community along with related features that are in there which might compliement `/src/Miadi/app` ...

 and if not enouhht , neither of them consume the packages that our MW has in relation to a "ceremony" !
